.site-meta {
  padding: 20px 0;
  color: white;
  background: #336699;

  +tablet() {
    box-shadow: 0 0 16px rgba(0,0,0,0.5);
  }
  +mobile() {
    box-shadow: 0 0 16px rgba(0,0,0,0.5);
  }
}

.brand {
  display: block;
  padding: 0;
  background: #336699;

  &:hover { color: white; }
}

.site-subtitle { margin: 0; }

.site-search form { display: none; }
